import React from "react";

/**
 * Komponent kontenera do generowania fiszek przez AI
 * (Implementacja zostanie uzupełniona w kolejnych krokach)
 */
export default function AIGenerationContainer() {
  return (
    <div className="space-y-6">
      <p className="text-muted-foreground">
        Funkcjonalność generowania fiszek przez AI zostanie zaimplementowana w kolejnym kroku.
      </p>
    </div>
  );
}
